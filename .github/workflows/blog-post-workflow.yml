name: Latest blog post workflow
on:
  schedule:
    # Runs every hour
    - cron: '0 * * * *'
jobs:
  update-readme-with-blog:
    name: Update this repo's README with latest blog posts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: jagdishsaini90/blog-post-workflow@master
        with:
          feed_list: 'https://dev.to/jagdishsaini90, https://hashnode.com/@jsaini8941'
